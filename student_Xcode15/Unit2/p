/*:
## Exercise - Methods
 
 A `Book` struct has been created for you below. Add an instance method on `Book` called `description` that will print out facts about the book.
 */
struct Book {
    var title: String
    var author: String
    var pages: Int
    var price: Double
    
    // Instance method to print book details
    func description() {
        print("Book Title: \(title), Author: \(author), Pages: \(pages), Price: $\(price)")
    }
}

// Create an instance of `Book` and call this method on that instance.
let myBook = Book(title: "The Great Gatsby", author: "F. Scott Fitzgerald", pages: 180, price: 10.99)
myBook.description()

/*:
 A `Post` struct has been created for you below, representing a generic social media post. Add a mutating method on `Post` called `like` that will increment `likes` by one.
 */
struct Post {
    var message: String
    var likes: Int
    var numberOfComments: Int
    
    // Mutating method to increment likes
    mutating func like() {
        likes += 1
    }
}

// Create an instance of `Post` and call `like()` on it.
var myPost = Post(message: "Hello, world!", likes: 0, numberOfComments: 0)

// Print likes before calling like()
print("Likes before: \(myPost.likes)")

// Call the like method
myPost.like()

// Print likes after calling like()
print("Likes after: \(myPost.likes)")
